'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

const templatizer = require('../templatizer.js');

const _path = require('path');

exports.builder = ({docs, writeFile, globals, theme}) => {
/*
    const fileName = 'manual/index.html';
    const content = templatizer.fromFile(_path.resolve(theme, "layout.html"), ["type", "docs", "doc"], globals, "layout.html")("class", docs, doc)
    writeFile(fileName, content);
*/
}
