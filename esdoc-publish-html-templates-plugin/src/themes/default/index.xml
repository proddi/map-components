<template id="index.html" args-as="index">
    ${render("debug-toggle", index)}
    ${render("debug-info", index)}
    <article>
        <div data-ice="index" class="github-markdown">${markdown(index.content)}</div>
    </article>
</template>
